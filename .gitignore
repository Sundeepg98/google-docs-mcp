# Secrets — never commit
credentials/
.env
.fly-token-temp.txt

# Python
__pycache__/
*.pyc
*.pyo
*.so
.venv/
venv/
*.egg-info/
build/
dist/

# Test/coverage artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
# CI-generated artifacts that get baked into the Docker image at deploy
# time (uploaded via actions/upload-artifact). Don't commit them.
test-results.json
mutation-check.json

# Editors
.idea/
.vscode/
